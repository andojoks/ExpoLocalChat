import type {ChatModel,TutorTurn} from './chat-model';
const MODEL='onnx-community/SmolLM2-135M-Instruct-ONNX';
const CDN='https://cdn.jsdelivr.net/npm/@huggingface/transformers@4.0.1';
const runtimeImport=(url:string)=>(new Function('url','return import(url)') as (url:string)=>Promise<any>)(url);
let transformers:any;
export function createChatModel():ChatModel{let generator:any,initializing:Promise<void>|undefined;return{name:'SmolLM2-135M-Instruct',async initialize(){if(generator)return;if(initializing)return initializing;initializing=(async()=>{transformers||=await runtimeImport(CDN);generator=await transformers.pipeline('text-generation',MODEL,{dtype:'q4'})})();return initializing},async generate(turns:TutorTurn[],onToken){await this.initialize();const prompt=turns.map(turn=>`<|im_start|>${turn.role}\n${turn.content}<|im_end|>`).join('\n')+'\n<|im_start|>assistant\n';let streamed='';const options:any={max_new_tokens:240,temperature:.35,do_sample:true,repetition_penalty:1.12,return_full_text:false};if(onToken){transformers||=await runtimeImport(CDN);options.streamer=new transformers.TextStreamer(generator.tokenizer,{skip_prompt:true,skip_special_tokens:true,callback_function:(text:string)=>{streamed+=text;onToken(text)}})}const output:any=await generator(prompt,options),generated=output?.[0]?.generated_text;return(typeof generated==='string'?generated:streamed).trim()}}}
