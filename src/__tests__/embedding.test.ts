import {describe,expect,it} from '@jest/globals';
import {cosine,HashEmbeddingProvider,EMBEDDING_DIMENSIONS} from '../ai/embeddings/embedding';
describe('local embedding fallback',()=>{const provider=new HashEmbeddingProvider();it('returns normalized vectors at the mobile MRL dimension',async()=>{const vector=await provider.embedQuery('quadratic algebra');expect(vector).toHaveLength(EMBEDDING_DIMENSIONS);expect(Math.sqrt(vector.reduce((sum,value)=>sum+value*value,0))).toBeCloseTo(1,8)});it('is deterministic and ranks matching text over unrelated text',async()=>{const query=await provider.embedQuery('solve quadratic algebra'),[matching,unrelated]=await provider.embedDocuments(['solve quadratic algebra equation','define osmosis cell membrane']);expect(await provider.embedQuery('solve quadratic algebra')).toEqual(query);expect(cosine(query,matching)).toBeGreaterThan(cosine(query,unrelated))})});
